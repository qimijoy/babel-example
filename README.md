A playground for Babel's testing.

Command ```npm run babel``` runs Babel compilation, creates dist folder width compiled ```src/main.js``` file. 

You 
can change ```.browserslist``` and re-run ```npm run babel``` to see, how compiled versions of ```main.js``` differs 
depends on browsers version.
